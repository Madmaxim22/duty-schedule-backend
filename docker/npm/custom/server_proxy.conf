# Подключается NPM в конец каждого proxy server block (/data/nginx/custom/).
# Лимит в UI (Advanced) иногда не применяется — этот файл надёжнее.
# 10 вложений × 8 МБ — см. duty-nginx nginx.conf
client_max_body_size 80m;
